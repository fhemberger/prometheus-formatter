{
  "name": "prometheus-formatter",
  "version": "3.1.0",
  "description": "Browser extension which makes plain Prometheus/OpenMetrics endpoints easier to read.",
  "scripts": {
    "test": "standard --fix",
    "release": "./package/package.sh",
    "lint-firefox": "npm run release && npx addons-linter --min-manifest-version=3 --max-manifest-version=3 extension-firefox"
  },
  "author": "Frederic Hemberger",
  "license": "MIT",
  "devDependencies": {
    "addons-linter": "^6.13.0",
    "standard": "^17.1.0"
  }
}
