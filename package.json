{
  "name": "chrome-prometheus-formatter",
  "version": "1.2.2",
  "description": "Chrome Extension which makes plain Prometheus metrics easier to read.",
  "scripts": {
    "test": "standard js/*.js --fix",
    "release": "cd extension && zip -r ../chrome-webstore-release.zip html icons js LICENSE.txt manifest.json -x icons/.DS_Store js/.DS_Store .DS_Store"
  },
  "author": "Frederic Hemberger",
  "license": "MIT",
  "devDependencies": {
    "standard": "14.3.1"
  }
}
